import { Box, Typography } from "@mui/material";

const Home: React.FC<{}> = ({}) => {
  return (
    <Box>
      <Typography>Welcome to the dashboard</Typography>
    </Box>
  );
};

export default Home;
